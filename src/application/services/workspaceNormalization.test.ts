import { describe,expect,it } from "vitest";import { normalizeWorkspaceName,normalizeWorkspaceSlug } from "./workspaceNormalization";describe("Workspace normalization",()=>{it("normalizes Unicode, whitespace, and case",()=>{expect(normalizeWorkspaceName("  Ｍain   Team ")).toBe("main team")});it("creates stable safe slugs",()=>{expect(normalizeWorkspaceSlug("  Main   Team  ")).toBe("main-team")});it("makes normalized collisions visible",()=>{expect(normalizeWorkspaceName("Main Team")).toBe(normalizeWorkspaceName("ＭＡＩＮ   TEAM"))})});
