import { describe,expect,it } from "vitest";function workspaceHref(id:string,organizationId:string){return `/workspaces/${encodeURIComponent(id)}?organizationId=${encodeURIComponent(organizationId)}`}describe("Workspace mobile navigation",()=>{it("builds tenant-scoped navigation URLs",()=>{expect(workspaceHref("workspace one","org/1")).toBe("/workspaces/workspace%20one?organizationId=org%2F1")});it("retains a direct editor route",()=>{expect("/editor").toBe("/editor")})});
